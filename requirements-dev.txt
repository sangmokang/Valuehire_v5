# Harness 검증 러너 고정 — verify.sh / pytest 재현용.
# 신규 환경: python3 -m venv .venv && .venv/bin/pip install -r requirements-dev.txt
# 기존: .venv-playwright 가 있으면 verify.sh 가 자동으로 그걸 우선 사용.
pytest>=9,<10
psycopg[binary]>=3.2,<4
# 일부 테스트가 가져오는 소스 모듈이 import 시점에 playwright를 당긴다(브라우저 실행은 안 함 — 가짜 매니저 사용).
# CI에서 ModuleNotFoundError 방지용. 브라우저 바이너리(playwright install)는 불필요.
playwright==1.60.0
# 조각 C — 디스코드 직결 게이트웨이(scripts/discord_direct_gateway.py) 실접속용.
# 버전 고정: docs/prompts/discord-direct-connect-goal-2026-07-17.md §5C.
discord.py==2.4.0
# discord.py 의 player.py 가 무조건 import audioop 하는데, 파이썬 3.13+ 는 audioop 를
# stdlib 에서 제거했다(PEP 594 폐기 유예 종료) — 백포트 패키지로 보강해야 import 자체가 된다.
audioop-lts==0.2.2
